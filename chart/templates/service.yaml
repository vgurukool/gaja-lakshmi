apiVersion: v1
kind: Service
metadata:
  name: {{ include "gaja-lakshmi.fullname" . }}
  labels:
    {{- include "gaja-lakshmi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gaja-lakshmi.selectorLabels" . | nindent 4 }}
